# Dotfiles
Personal Linux configuration files managed with GNU Stow.

## Usage
### Apply a config:
cd ~/dotfiles
stow helix

### Remove a config:
stow -D helix

## Note
### You can preview changes before applying:
stow -n -v helix

